import { RootState } from '../'


export const chat = (state: RootState) => state.chat
export const importStatement = (state: RootState) => state.incomeStatement
export const balance = (state: RootState) => state.balance